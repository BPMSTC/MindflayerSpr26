// Development environment settings.
// `apiBaseUrl` points to `/api` so Angular dev-server proxy can forward
// requests to the backend without CORS friction during local development.
export const environment = {
  production: false,
  apiBaseUrl: '/api',
};
