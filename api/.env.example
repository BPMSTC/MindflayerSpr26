# HTTP port for the API server process.
PORT=5000

# MongoDB connection string used by Mongoose at startup.
# Local default points to database named `catmon`.
MONGODB_URI=mongodb://localhost:27017/catmon

# Frontend origin allowed by CORS when running in the browser.
# For Angular dev server this is typically localhost:4200.
CLIENT_ORIGIN=http://localhost:4200
